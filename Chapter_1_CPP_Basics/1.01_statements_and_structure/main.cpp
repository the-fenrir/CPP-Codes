// 1.1 — minimal program; every token annotated.
#include <iostream>          // preprocessor directive: pulls in I/O facilities. No ';'.

int main()                   // the entry point. Must return int. Exactly one per program.
{
    std::cout << "Hello, world!\n";  // a statement: prints text. Ends with ';'.
    return 0;                        // a statement: returns success to the OS.
}
